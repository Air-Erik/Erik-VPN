# 🚀 РАЗВЕРТЫВАНИЕ НА VPS: 79.137.194.141

**Дата:** 29 декабря 2025
**VPS:** 79.137.194.141
**Статус:** Docker установлен, переносим файлы

---

## 📦 СПОСОБ 1: ПЕРЕНОС ЧЕРЕЗ SCP (Рекомендуется)

### Из Windows (PowerShell):

```powershell
# Перейдите в папку проекта на вашем компьютере
cd D:\Repos\Erik-VPN

# Создайте архив проекта (без лишнего)
# Если есть 7zip:
7z a erik-vpn.zip * -x!.git -x!data -x!logs -x!node_modules

# Или через Compress-Archive (встроено в Windows):
Compress-Archive -Path * -DestinationPath erik-vpn.zip -Force
```

```powershell
# Загрузите на VPS через SCP
scp erik-vpn.zip root@79.137.194.141:/opt/
```

### На VPS (подключитесь по SSH):

```bash
# Подключение
ssh root@79.137.194.141
# Пароль: ZiwEeQ8JaFhO

# Распаковка
cd /opt
apt install -y unzip
unzip erik-vpn.zip -d Erik-VPN
cd Erik-VPN

# Создайте необходимые папки
mkdir -p data/3x-ui logs certs

# Проверьте что .env файл на месте
ls -la .env
cat .env
```

---

## 📦 СПОСОБ 2: ЧЕРЕЗ WinSCP (Графический интерфейс)

### Установка WinSCP:

1. Скачайте: https://winscp.net/eng/download.php
2. Установите

### Подключение:

1. Откройте WinSCP
2. Новое подключение:
   - Протокол: SFTP
   - Хост: 79.137.194.141
   - Порт: 22
   - Пользователь: root
   - Пароль: ZiwEeQ8JaFhO
3. Нажмите "Войти"

### Перенос файлов:

1. Слева (локальный компьютер): `D:\Repos\Erik-VPN`
2. Справа (VPS): создайте папку `/opt/Erik-VPN`
3. Перетащите файлы слева направо:
   - ✅ docker-compose.yml
   - ✅ .env
   - ✅ .gitignore
   - ✅ scripts/
   - ✅ docs/
   - ❌ НЕ переносите: .git/, data/, logs/, node_modules/

---

## 📦 СПОСОБ 3: ПРЯМОЕ СОЗДАНИЕ ФАЙЛОВ (Быстрый)

Если файлов мало, можно создать их прямо на VPS:

```bash
# Подключитесь к VPS
ssh root@79.137.194.141

# Создайте структуру
cd /opt
mkdir -p Erik-VPN
cd Erik-VPN

# Создайте docker-compose.yml
cat > docker-compose.yml << 'EOF'
[содержимое docker-compose.yml]
EOF

# Создайте .env (скопируйте с локального)
nano .env
# Вставьте содержимое вашего .env файла
# Ctrl+O, Enter, Ctrl+X

# Создайте папки
mkdir -p data/3x-ui logs certs scripts docs
```

---

## 🚀 ПОСЛЕ ПЕРЕНОСА ФАЙЛОВ

### 1. Проверка файлов на VPS:

```bash
cd /opt/Erik-VPN

# Проверьте что все на месте
ls -la

# Должны быть:
# - docker-compose.yml
# - .env
# - data/
# - logs/
```

### 2. Проверка .env:

```bash
cat .env
```

**Убедитесь что установлены:**
- `SERVER_HOST=79.137.194.141`
- `ADMIN_PASSWORD=ваш_пароль` (НЕ дефолтный!)

### 3. Настройка Firewall:

```bash
# Откройте необходимые порты
ufw allow 22/tcp
ufw allow 2053/tcp
ufw allow 443/tcp
ufw allow 8080/tcp
ufw --force enable

# Проверка
ufw status
```

### 4. Запуск Erik-VPN:

```bash
cd /opt/Erik-VPN

# Запуск
docker compose up -d

# Проверка
docker compose ps
```

**Ожидаемый вывод:**
```
NAME        IMAGE                              STATUS
erik-vpn    ghcr.io/mhsanaei/3x-ui:latest     Up (healthy)
```

### 5. Проверка логов:

```bash
docker compose logs --tail=50
```

Не должно быть ошибок (ERROR).

### 6. Доступ к панели:

Откройте в браузере:
```
http://79.137.194.141:2053
```

**Логин:** admin
**Пароль:** (из вашего .env файла)

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

- [ ] Файлы перенесены на VPS
- [ ] .env файл настроен (ADMIN_PASSWORD изменен)
- [ ] Firewall настроен (порты открыты)
- [ ] Docker compose up -d выполнен
- [ ] Контейнер запущен (docker compose ps)
- [ ] Панель доступна (http://79.137.194.141:2053)
- [ ] Успешный вход в панель

---

## 🚨 TROUBLESHOOTING

### Проблема: Не могу загрузить файлы через SCP

**Решение:**
```bash
# На VPS создайте папку
ssh root@79.137.194.141
mkdir -p /opt/Erik-VPN

# Используйте WinSCP (графический интерфейс)
```

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Проверьте логи
docker compose logs

# Проверьте что порты свободны
netstat -tulpn | grep -E '(2053|443|8080)'

# Перезапуск
docker compose down
docker compose up -d
```

### Проблема: Панель недоступна

**Решение:**
```bash
# Проверьте firewall
ufw status

# Проверьте контейнер
docker compose ps

# Проверьте локально на VPS
curl -I http://localhost:2053
```

---

## 📞 СЛЕДУЮЩИЕ ШАГИ

После успешного запуска панели:
1. Войти в 3X-UI
2. Настроить VLESS + Reality
3. Настроить VLESS + XHTTP
4. Экспортировать клиентские конфигурации

Сообщите когда панель откроется! 🎯
