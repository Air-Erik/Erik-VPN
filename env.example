# Erik-VPN Configuration
# Этот файл - пример. Скопируйте в .env: cp env.example .env

# =============================================================================
# ИНФОРМАЦИЯ О СЕРВЕРЕ (для документации)
# =============================================================================

# IP адрес вашего VPS
SERVER_HOST=your-vps-ip-here

# Часовой пояс
TZ=Europe/Moscow

# =============================================================================
# ВАЖНО: НАСТРОЙКИ 3X-UI
# =============================================================================

# 3X-UI НЕ поддерживает переменные окружения для логина/пароля!
# Учётные данные настраиваются ТОЛЬКО через веб-панель:
#   Panel Settings → Authentication
#
# После первого запуска:
# 1. Откройте http://YOUR_VPS_IP:2053
# 2. Войдите с admin / admin
# 3. СРАЗУ измените логин и пароль в настройках панели!

# =============================================================================
# ПОРТЫ (для справки, настраиваются в панели)
# =============================================================================

# При network_mode: host контейнер использует порты хоста напрямую:
# - 2053 - Панель управления 3X-UI
# - 443  - VLESS + Reality (настроить в панели)
# - 8080 - VLESS + XHTTP (настроить в панели)

# =============================================================================
# VLESS + REALITY (для справки)
# =============================================================================

# Рекомендуемые домены для маскировки Reality:
# - microsoft.com
# - apple.com
# - cloudflare.com
# - www.google.com

# =============================================================================
# КОМАНДЫ
# =============================================================================

# Запуск:
#   docker compose up -d
#
# Остановка:
#   docker compose down
#
# Обновление:
#   docker compose down
#   docker compose pull
#   docker compose up -d
#
# Логи:
#   docker compose logs -f
