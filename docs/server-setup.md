# 🚀 Установка Erik-VPN на VPS

Пошаговая инструкция развертывания Erik-VPN сервера.

---

## 📋 Требования

### VPS сервер:
- **ОС:** Ubuntu 22.04 / Debian 11+
- **RAM:** 1GB минимум (рекомендуется 2GB)
- **CPU:** 1 vCore (рекомендуется 2)
- **Диск:** 10GB
- **Сеть:** Публичный IPv4, 100+ Mbps

### Локальный компьютер:
- SSH клиент (встроен в Windows 10+, Linux, macOS)
- Git (опционально)

---

## 🎯 ШАГ 1: ПОКУПКА VPS

### Рекомендуемый провайдер: Aeza.net

1. Перейдите на https://aeza.net/
2. Регистрация → VPS
3. Выберите конфигурацию:
   ```
   VPS-2: 249₽/мес
   - 2 vCPU
   - 2GB RAM
   - 30GB SSD
   - Локация: Нидерланды
   - ОС: Ubuntu 22.04
   ```
4. Оплатите картой РФ
5. Получите данные доступа (IP, пароль)

**Другие провайдеры:** См. [VPS_PROVIDERS.md](../VPS_PROVIDERS.md)

---

## 🔐 ШАГ 2: ПОДКЛЮЧЕНИЕ К VPS

### Подключение по SSH:

**Windows (PowerShell):**
```powershell
ssh root@YOUR_VPS_IP
```

**Linux/macOS (Terminal):**
```bash
ssh root@YOUR_VPS_IP
```

При первом подключении:
```
The authenticity of host '...' can't be established.
Are you sure you want to continue connecting (yes/no)? yes
```

Введите пароль из письма от провайдера.

### Смена пароля root (рекомендуется):
```bash
passwd
# Введите новый пароль дважды
```

---

## 🛠️ ШАГ 3: ПОДГОТОВКА СИСТЕМЫ

### Обновление пакетов:
```bash
apt update && apt upgrade -y
```

### Установка необходимых утилит:
```bash
apt install -y curl wget git ufw
```

### Настройка часового пояса:
```bash
timedatectl set-timezone Europe/Moscow
```

### Проверка:
```bash
date
# Должна показать московское время
```

---

## 🐳 ШАГ 4: УСТАНОВКА DOCKER

### Автоматическая установка:
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
```

### Проверка установки:
```bash
docker --version
# Вывод: Docker version 24.x.x

docker compose version
# Вывод: Docker Compose version v2.x.x
```

### Включение автозапуска:
```bash
systemctl enable docker
systemctl start docker
```

---

## 📦 ШАГ 5: УСТАНОВКА ERIK-VPN

### Клонирование репозитория:

**Вариант A: Через Git (если есть)**
```bash
cd /opt
git clone https://github.com/your-username/Erik-VPN.git
cd Erik-VPN
```

**Вариант B: Загрузка ZIP**
```bash
cd /opt
wget https://github.com/your-username/Erik-VPN/archive/refs/heads/main.zip
unzip main.zip
mv Erik-VPN-main Erik-VPN
cd Erik-VPN
```

### Создание конфигурации:
```bash
cp env.example .env
nano .env
```

### Настройка .env файла:

**Обязательно измените:**
```bash
# IP адрес вашего VPS
SERVER_HOST=YOUR_VPS_IP

# Пароль администратора (ОБЯЗАТЕЛЬНО!)
ADMIN_PASSWORD=YourSecurePassword123!
```

**Сохранение в nano:**
- `Ctrl + O` → Enter (сохранить)
- `Ctrl + X` (выйти)

---

## 🔥 ШАГ 6: НАСТРОЙКА FIREWALL

### Открытие необходимых портов:
```bash
# SSH (чтобы не потерять доступ)
ufw allow 22/tcp

# 3X-UI панель управления
ufw allow 2053/tcp

# VLESS + Reality (основной протокол)
ufw allow 443/tcp

# VLESS + XHTTP (резервный протокол)
ufw allow 8080/tcp

# Включение firewall
ufw --force enable

# Проверка
ufw status
```

**Вывод должен быть:**
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
2053/tcp                   ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
8080/tcp                   ALLOW       Anywhere
```

---

## 🚀 ШАГ 7: ЗАПУСК ERIK-VPN

### Запуск контейнера:
```bash
docker compose up -d
```

**Вывод:**
```
[+] Running 2/2
 ✔ Network erik-vpn_erik-vpn-net  Created
 ✔ Container erik-vpn             Started
```

### Проверка статуса:
```bash
docker compose ps
```

**Должно быть:**
```
NAME        IMAGE                              STATUS
erik-vpn    ghcr.io/mhsanaei/3x-ui:latest     Up (healthy)
```

### Просмотр логов:
```bash
docker compose logs -f
```

**Нажмите `Ctrl + C` для выхода из логов.**

---

## 🎛️ ШАГ 8: ДОСТУП К ПАНЕЛИ УПРАВЛЕНИЯ

### Открытие панели 3X-UI:

1. Откройте браузер
2. Перейдите по адресу:
   ```
   http://YOUR_VPS_IP:2053
   ```

3. Введите данные:
   ```
   Логин: admin
   Пароль: (из .env файла)
   ```

4. Вы должны увидеть панель управления 3X-UI

---

## ✅ ШАГ 9: ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### Проверка контейнера:
```bash
# Статус
docker compose ps

# Логи (последние 50 строк)
docker compose logs --tail=50

# Использование ресурсов
docker stats erik-vpn --no-stream
```

### Проверка портов:
```bash
netstat -tulpn | grep -E '(2053|443|8080)'
```

**Должно показать:**
```
tcp6  0  0  :::2053   :::*  LISTEN  docker-proxy
tcp6  0  0  :::443    :::*  LISTEN  docker-proxy
tcp6  0  0  :::8080   :::*  LISTEN  docker-proxy
```

### Проверка доступности панели:
```bash
curl -I http://localhost:2053
```

**Должно вернуть:**
```
HTTP/1.1 200 OK
```

---

## 🎉 ГОТОВО!

Сервер Erik-VPN установлен и работает!

### Следующие шаги:

1. **Настроить протоколы:**
   - [Конфигурация VLESS + Reality](server-config.md)
   - [Конфигурация VLESS + XHTTP](server-config.md#xhttp)

2. **Настроить клиенты:**
   - [Android](android.md)
   - [iOS](ios.md)
   - [Windows](windows.md)

3. **Тестирование:**
   - Подключиться с клиента
   - Проверить доступ к заблокированным сайтам
   - Провести speedtest

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

### Управление контейнером:
```bash
# Перезапуск
docker compose restart

# Остановка
docker compose down

# Запуск
docker compose up -d

# Обновление образа
docker compose pull
docker compose up -d
```

### Просмотр информации:
```bash
# Логи в реальном времени
docker compose logs -f

# Использование ресурсов
docker stats

# Информация о контейнере
docker inspect erik-vpn
```

### Резервное копирование:
```bash
# Бэкап данных 3X-UI
tar -czf backup-$(date +%Y%m%d).tar.gz data/

# Восстановление
tar -xzf backup-YYYYMMDD.tar.gz
```

---

## 🚨 TROUBLESHOOTING

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Проверить логи
docker compose logs

# Проверить порты (может быть занят)
netstat -tulpn | grep -E '(2053|443|8080)'

# Перезапустить
docker compose down
docker compose up -d
```

### Проблема: Панель недоступна

**Решение:**
```bash
# Проверить firewall
ufw status

# Проверить что контейнер запущен
docker compose ps

# Проверить с сервера
curl -I http://localhost:2053
```

### Проблема: Нет интернета на клиенте

**Решение:**
```bash
# Проверить IP forwarding
sysctl net.ipv4.ip_forward
# Должно быть: net.ipv4.ip_forward = 1

# Если 0, включить:
echo 'net.ipv4.ip_forward=1' >> /etc/sysctl.conf
sysctl -p
```

---

## 📚 ДОПОЛНИТЕЛЬНО

- [VPS провайдеры](../VPS_PROVIDERS.md)
- [Конфигурация протоколов](server-config.md)
- [Troubleshooting](troubleshooting.md)
- [FAQ](../README.md#faq)

---

**Установка завершена! Переходите к настройке протоколов.** 🎯
