# ⚙️ Конфигурация VLESS протоколов

Настройка VLESS + Reality и VLESS + XHTTP в панели 3X-UI.

---

## 🔐 ШАГ 1: ПЕРВЫЙ ВХОД И СМЕНА ПАРОЛЯ

### 1. Откройте панель:

```
http://YOUR_VPS_IP:2053
```
Если вы меняли порт в `.env` (`PANEL_PORT`) — используйте его вместо `2053`.

### 2. Войдите с дефолтными credentials:

```
Логин: admin
Пароль: admin
```

### 3. Смените пароль через консоль:

**На VPS выполните:**
```bash
cd /root/erik-vpn

# Измените username и password
docker exec -it erik-vpn x-ui setting -username ваш_логин -password "ваш_пароль"

# Перезапустите
docker compose restart
```

**Или через панель:**
1. Settings → Panel Settings
2. Измените Username и Password
3. Save

---

## 🛡️ ШАГ 2: НАСТРОЙКА VLESS + REALITY (Основной протокол)

### 1. В панели 3X-UI:

Перейдите: **Inbounds → Add Inbound**

### 2. Основные параметры:

```
Remark: VLESS-Reality-Main
Protocol: VLESS
Listen IP: (оставьте пустым)
Port: 443
```

### 3. Client Settings:

Нажмите **Add Client**:
```
Email: user@erik-vpn
ID: (генерируется автоматически, оставьте как есть)
Flow: xtls-rprx-vision
Limit IP: 2 (максимум 2 устройства на один ключ)
Total GB: 0 (без ограничений)
Expiry Time: 0 (бессрочно)
```

### 4. Stream Settings:

```
Network: tcp
Security: reality
```

### 5. Reality Settings:

Нажмите **Get New Cert** для генерации ключей Reality.

```
Dest: microsoft.com:443
Server Names: microsoft.com
Private Key: (генерируется автоматически)
Short IDs: (генерируется автоматически, или укажите: 6ba85179e30d4fc2)
```

**Важные параметры Reality:**
- **Dest** - реальный сайт для маскировки (microsoft.com, apple.com, cloudflare.com)
- **Server Names (SNI)** - домен который видит DPI
- **Private Key** - секретный ключ сервера (НЕ показывайте никому!)
- **Short IDs** - короткие идентификаторы для валидации клиентов

### 6. Другие настройки:

```
Sniffing: включено (Enabled)
- Destination Override: HTTP, TLS, QUIC, FAKEDNS
```

### 7. Сохраните:

Нажмите **Add** внизу страницы.

---

## 🌐 ШАГ 3: НАСТРОЙКА VLESS + XHTTP (Резервный протокол)

### 1. Добавьте новый Inbound:

**Inbounds → Add Inbound**

### 2. Основные параметры:

```
Remark: VLESS-XHTTP-Backup
Protocol: VLESS
Listen IP: (пустое)
Port: 8080
```

### 3. Client Settings:

```
Email: user@erik-vpn
ID: (используйте тот же UUID что и для Reality, или новый)
Flow: none (для XHTTP flow не нужен)
Limit IP: 2
Total GB: 0
Expiry Time: 0
```

### 4. Stream Settings:

```
Network: xhttp
Security: none
```

### 5. XHTTP Settings:

```
Path: /xhttp (или любой путь)
Host: (пустое, или ваш домен)
```

### 6. Sniffing:

```
Sniffing: Enabled
Destination Override: HTTP, TLS, QUIC
```

### 7. Сохраните.

---

## 📤 ШАГ 4: ЭКСПОРТ КЛИЕНТСКИХ КОНФИГУРАЦИЙ

### 1. В списке Inbounds найдите ваши конфигурации

### 2. Для каждой конфигурации:

**Нажмите на иконку клиента (человечек):**
- QR Code - для сканирования на мобильных
- Copy Link - для вставки в приложения
- Export - JSON конфигурация

### 3. Сохраните:

**VLESS + Reality:**
```
Файл: reality-client.txt
Содержимое: vless://UUID@YOUR_VPS_IP:443?...
```

**VLESS + XHTTP:**
```
Файл: xhttp-client.txt
Содержимое: vless://UUID@YOUR_VPS_IP:8080?...
```

---

## 🧪 ШАГ 5: ТЕСТИРОВАНИЕ

### 1. Проверьте что Inbound работает:

В списке Inbounds должен быть статус: **Active (зеленый)**

### 2. Проверьте XRAY Core:

**Settings → Xray Configurations**

Убедитесь что Xray запущен:
```
Status: Running
Version: 25.10.15 или новее
```

### 3. Проверьте логи:

**Panel Settings → Logs**

Не должно быть ошибок (ERROR).

### 4. Тест с клиента:

- Установите приложение (Hiddify, v2rayNG)
- Импортируйте конфигурацию
- Подключитесь
- Откройте: https://whoer.net
- IP должен измениться на IP вашего VPS

---

## 🎨 РЕКОМЕНДУЕМЫЕ SNI ДЛЯ REALITY

| Домен | Плюсы | Минусы |
|-------|-------|--------|
| **microsoft.com** | ✅ Очень популярный, стабильный | - |
| **apple.com** | ✅ Высокий трафик, надежный | - |
| **cloudflare.com** | ✅ CDN провайдер, много соединений | - |
| **www.google.com** | ⚠️ Популярный | ⚠️ Может быть перегружен |
| **github.com** | ✅ Техническая аудитория | ⚠️ Иногда меняет конфигурацию |
| **aws.amazon.com** | ✅ Облачный провайдер, B2B трафик | - |

**Рекомендация:** Используйте **microsoft.com** - самый стабильный выбор.

---

## 🔐 БЕЗОПАСНОСТЬ

### 1. Ограничение IP:

В настройках клиента:
```
Limit IP: 2
```
Один ключ максимум на 2 устройства.

### 2. Ограничение трафика (опционально):

```
Total GB: 100
```
100GB в месяц на клиента.

### 3. Срок действия (опционально):

```
Expiry Time: 30 days
```
Ключ истечет через 30 дней.

### 4. Отключение неиспользуемых Inbound:

Если протокол не используется - отключите его (Toggle switch).

---

## 📊 МОНИТОРИНГ

### 1. Статистика трафика:

**Dashboard** показывает:
- Общий трафик (Upload/Download)
- Активные соединения
- Использование CPU/RAM

### 2. Логи подключений:

**Panel Settings → System Logs**

Показывает:
- Успешные подключения
- Ошибки аутентификации
- IP адреса клиентов

### 3. Статистика по клиентам:

В списке Inbounds → иконка клиента:
- Total Traffic (всего передано)
- Upload / Download
- Last Connection (последнее подключение)

---

## 🚨 TROUBLESHOOTING

### Проблема: Inbound не запускается

**Решение:**
1. Проверьте что порт не занят: `netstat -tulpn | grep 443`
2. Проверьте firewall: `ufw status`
3. Проверьте логи Xray: Settings → Logs

### Проблема: Клиент не подключается

**Решение:**
1. Проверьте что Inbound Active (зеленый)
2. Проверьте firewall на VPS: `ufw allow 443/tcp`
3. Проверьте правильность конфигурации клиента
4. Проверьте логи на сервере

### Проблема: Подключение медленное

**Решение:**
1. Проверьте нагрузку сервера: `docker stats erik-vpn`
2. Попробуйте другой SNI (cloudflare.com вместо microsoft.com)
3. Проверьте speedtest на VPS: `speedtest-cli`

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

- [ ] VLESS + Reality Inbound создан
- [ ] VLESS + XHTTP Inbound создан
- [ ] Reality ключи сгенерированы
- [ ] SNI настроен (microsoft.com)
- [ ] Клиентские конфигурации экспортированы
- [ ] QR коды созданы
- [ ] Firewall открыт (443, 8080)
- [ ] Тестовое подключение успешно

---

## 📱 СЛЕДУЮЩИЕ ШАГИ

После настройки протоколов:
1. [Настройка Android клиентов](android.md)
2. [Настройка iOS клиентов](ios.md)
3. [Настройка Windows клиентов](windows.md)
4. [Тестирование и безопасность](troubleshooting.md)

---

**Конфигурация завершена!** 🎉
